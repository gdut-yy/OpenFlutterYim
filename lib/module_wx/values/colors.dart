import 'dart:ui';

/// YIM 颜色
/// 参考自 微信 7.0
class YimColor {
  YimColor._();

  // ============ appBar ============
  static const Color appBarBackground = Color(0xFFededed);

  static const Color appBarText = Color(0xFF181818);

  static const Color appBarIcon = Color(0xFF000000);

  // ============ bottomBar ============
  static const Color bottomBarBackground = Color(0xFFf7f7f7);

  static const Color bottomBarPressed = Color(0xFF08c161);

  static const Color bottomBarNormal = Color(0xFF000000);

  // ============ body ============
  static const Color bodyBackground = Color(0xFFededed);

  // ============ items ============
  /// 通用白
  static const Color itemsNormalBackground = Color(0xFFffffff);

  static const Color itemsPressedBackground = Color(0xFFd5d5d5);

  static const Color itemsTitle = Color(0xFF353535);

  static const Color itemsSubTitle = Color(0xFF999999);

  static const Color itemsTime = Color(0xFFbbbbbb);

  static const Color itemsArrow = Color(0xFFacacac);

  static const Color itemsLine = Color(0xFFd9d9d9);

  // ============ popupMenu ============
  static const Color popupMenuText = Color(0xFFffffff);

  static const Color popupMenuBackground = Color(0xFF4c4c4c);

  // ============ slideBarBackground ============
  static const Color slideBarPressedBackground = Color(0xFFbfbfbf);

  static const Color slideBarText = Color(0xFF565656);

  static const Color slideBarHintsBackground = Color(0xFF808080);

  static const Color slideBarHintsText = Color(0xFFffffff);

  // ============ suspension 联系人列表上方的字母栏 ============
  static const Color suspensionBackground = Color(0xFFededed);

  static const Color suspensionText = Color(0xFF888888);

  // ============ a ============
  static const Color searchOption = Color(0xFF576b95);

  static const Color searchHints = Color(0xFFb1b1b1);

  static const Color searchLine = Color(0xFFd3d3d3);

  static const Color searchReturn = Color(0xFF171717);

  // ============ pay ============
  static const Color payGreen = Color(0xFF3bac6a);

  static const Color payAmountText = Color(0xFF9dd6b5);

  static const Color paySubTitleText = Color(0xFF716F76);

  // ============ detail ============
  static const Color detailAppBar = Color(0xFFf7f7f7);

  static const Color detailTitleText = Color(0xFF191919);

  static const Color detailSubTitleText = Color(0xFF999999);

  static const Color detailButton = Color(0xFF576b95);
}
